# PRD: Foundation — Next.js + Tailwind v4 + App Shell

## Tasks

- [ ] Install dependencies and update scripts. Run `npm install next@^15 react@^19 react-dom@^19 framer-motion@^11` followed by `npm install -D tailwindcss@^4 @tailwindcss/postcss@^4 postcss@^8 @types/react@^19 @types/react-dom@^19 @types/node@^22`, then update `package.json` scripts to add `"dev": "next dev"`, `"build": "next build"`, and `"start": "next start"` while keeping existing test, lint, check-types scripts intact. `[test: npx tsc --noEmit]`
- [ ] Update TypeScript configuration for Next.js. Modify `tsconfig.json`: set `module` to `esnext`, `moduleResolution` to `bundler`, `jsx` to `preserve`, `noEmit` to `true`, add `"lib": ["dom","dom.iterable","esnext"]`, add `plugins: [{ "name": "next" }]`, add `paths: { "@/*": ["./*"] }`, set `include` to `["next-env.d.ts","**/*.ts","**/*.tsx",".next/types/**/*.ts"]`, keep `exclude` as `["node_modules",".next","dist","out"]`. `[test: npx tsc --noEmit]`
- [ ] Append build artifacts to gitignore. Add `.next/`, `out/`, and `next-env.d.ts` lines to the end of `.gitignore`. `[test: bash scripts/append-gitignore.sh]`
- [ ] Delete old entry point file. Remove `src/index.ts` and verify it no longer exists on disk with `ls src/index.ts` returning error, ensuring nothing imports it. `[test: npx tsc --noEmit]`
- [ ] Create Next.js static export configuration. Create `next.config.mjs` exporting default config object with `output: 'export'`, `basePath: '/ralph-node-resume'`, `assetPrefix: '/ralph-node-resume'`, `images: { unoptimized: true }`, `trailingSlash: true`, and `reactStrictMode: true`. `[test: npx tsc --noEmit]`
- [ ] Create PostCSS configuration for Tailwind v4. Create `postcss.config.mjs` exporting default object `{ plugins: { '@tailwindcss/postcss': {} } }`. `[test: npx tsc --noEmit]`
- [ ] Copy profile image and disable Jekyll processing. Binary copy existing `profile.png` from repo root to `public/profile.png`, create empty file at `public/.nojekyll` using `touch public/.nojekyll`. `[test: bash scripts/setup-public-assets.sh]`
- [ ] Create base path helper for asset URLs. Create `src/lib/basePath.ts` exporting constant `BASE_PATH = '/ralph-node-resume'` and function `withBasePath(path: string): string` that returns `${BASE_PATH}${path.startsWith('/') ? path : '/' + path}` for all public asset references in JSX. `[test: npx jest tests/unit/create-basepath-helper.test.ts]`
- [ ] Create typed resume content data structure. Create `src/content/resume.ts` exporting: `profile` object with name, title, tagline, location, email, phone, linkedin, github, summary fields; `skills` array of `{ category, items }` objects for Frontend/AI/Infra/Backend categories; `experience` array of company objects with company, location, role, period, bullets (label/body), stack in reverse chronological order; `education` array with title, detail, optional status. Values must exactly match `resume.md`. `[test: npx jest tests/unit/load-resume-content.test.ts]`
- [ ] Create global CSS with Tailwind v4 theme configuration. Create `app/globals.css` importing Tailwind via `@import "tailwindcss"`, defining `@theme` block with `--font-sans: "Inter", system-ui, sans-serif;` and neutral palette tokens (`--color-bg: #ffffff`, `--color-fg: #0a0a0a`, `--color-muted: #6b7280`, `--color-subtle: #f5f5f7`, `--color-border: #e5e5ea`), base resets (html/body using CSS variables, smooth scroll behavior via `html { scroll-behavior: smooth; }`), and `.section-pad` utility class with `py-24 md:py-32 px-6 md:px-12 max-w-6xl mx-auto`. `[test: npx playwright test tests/e2e/apply-tailwind-theme.spec.ts]`
- [ ] Create root layout component. Create `app/layout.tsx` as server component with default export `RootLayout({ children })` rendering `<html lang="en">` using Inter font from `next/font/google` (subsets: ['latin']) applied to html element, `<body data-testid="app-body">` containing `<Nav />` and `{children}`, importing `./globals.css`, exporting metadata with title `"Paul-Valentin Mini — Lead Frontend Engineer"` and matching description. `[test: npx playwright test tests/e2e/create-root-layout.spec.ts]`
- [ ] Create sticky navigation component. Create `src/components/Nav.tsx` as client component (`'use client'`) rendering sticky top nav (`position: sticky; top: 0; z-index: 50`, white blurred background via `backdrop-blur bg-white/70 border-b border-[var(--color-border)]`) with `data-testid="nav"`. Render brand text "Paul-Valentin Mini" on left (`data-testid="nav-brand"`), anchor links for #profile, #skills, #experience, #education with testIds `nav-link-profile`, `nav-link-skills`, `nav-link-experience`, `nav-link-education` using default browser smooth scroll. Mobile-first: hamburger button (`data-testid="nav-toggle"`) below md breakpoint toggling dropdown panel (`data-testid="nav-menu"`). `[test: npx playwright test tests/e2e/build-sticky-nav.spec.ts]`
- [ ] Create reusable section wrapper component. Create `src/components/Section.tsx` as client or server component accepting props `{ id: string, title?: string, testId: string, children: React.ReactNode }`, rendering `<section id={id} data-testid={testId}>` with `.section-pad` class; if title provided, render `<h2 data-testid={`${testId}-title`}>` using large Apple-style typography (`text-4xl md:text-6xl font-semibold tracking-tight mb-12`). `[test: npx playwright test tests/e2e/create-section-wrapper.spec.ts]`
- [ ] Create home page with placeholder sections. Create `app/page.tsx` as server component rendering `<main data-testid="home">` containing sibling `<Section>` stubs for #hero, #profile, #skills, #experience, #education (each using Section with empty children and testIds: `section-hero`, `section-profile`, `section-skills`, `section-experience`, `section-education`). `[test: npx playwright test tests/e2e/render-home-page.spec.ts]`
